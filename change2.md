# change2.md — spec13.md 与 spec12.md 差异对比

> 本文档记录 spec13.md 相对于 spec12.md 的所有变更，按类别分类。

---

## 一、表结构变更（重大）

### 1.1 7 张核心业务表完全不同

| spec12.md | spec13.md | 变更说明 |
|-----------|-----------|----------|
| teams | Team | 命名风格从蛇形改为大驼峰 |
| members | User | **表名和语义变更**：members 改为 User，从"团队成员"变为独立用户实体 |
| commits | TeamMember | **新增中间表**：Team 和 User 的多对多中间表，带角色等元数据 |
| repositories | Commit | 保留，但关联从 member 改为 User |
| pull_requests | Repo | 从 repositories 简化为 Repo |
| reviews | RepoMember | **新增中间表**：Repo 和 User 的多对多中间表 |
| commit_stats | AuditLog | **表语义完全变更**：从提交统计改为审计日志 |

**关键差异：**
- spec12 中 members 直接属于 team（一对多），spec13 中 Team 和 User 是**多对多关系**，通过 TeamMember 中间表连接。
- spec13 新增了 **RepoMember 中间表**，表示仓库成员关系。
- spec13 删除了 `pull_requests`、`reviews`、`commit_stats` 三张表，新增了 `AuditLog` 审计日志表。
- spec13 强调中间表必须带元数据和索引。

---

## 二、技术栈变更

| 项目 | spec12.md | spec13.md | 变更说明 |
|------|-----------|-----------|----------|
| 数据库版本 | PostgreSQL（未指定版本） | **PostgreSQL 16** | 明确指定版本为 16 |
| 数据库部署 | 未指定 | Docker 或 Supabase 托管 | 新增部署方式要求 |
| ORM 模式 | SQLAlchemy 2.x（同步） | **SQLAlchemy 2.0+ async** | **从同步改为异步模式** |
| 模型目录 | `app/models/` | `src/models/` | 目录路径变更 |
| MCP 环境限制 | 未明确 | **只连开发/测试环境，不准连生产库** | 新增环境隔离要求 |
| 默认权限 | 未提及 | **必须执行 ALTER DEFAULT PRIVILEGES** | 新增数据库权限配置要求 |

---

## 三、阶段流程变更

### 3.1 阶段数量和内容对比

| 阶段 | spec12.md | spec13.md | 变更说明 |
|------|-----------|-----------|----------|
| S1 | 数据库设计（ERD） | 画数据库结构图 | 基本一致，但表不同 |
| S2 | SQLAlchemy 模型定义 | 落地 SQLAlchemy 模型 | **新增 async 模式要求**，目录改为 src/models/ |
| S3 | Alembic Migration | 生成 Migration 脚本 | 基本一致 |
| S4 | 种子数据 seed.py | 灌测试数据 | 数据量从"≥50条"改为"50-100条"；明确每团5-10人、每人3-5条提交 |
| S5 | 5 个核心查询 | 封装 5 个业务查询 | **查询内容不同**（见下方详细对比） |
| S6 | MCP 配置与只读用户 | **接口直连数据库 + MCP 配置（合并）** | spec12 的 S6（MCP配置）和 S7（接口直连）**合并为一个阶段**；新增 ALTER DEFAULT PRIVILEGES 要求 |
| S7 | 接口直连数据库 | 写 MCP 安全约束 | **阶段内容完全变更**：从接口开发改为在 CLAUDE.md 中写安全约束 |
| S8 | 敏感表声明 + 演示视频 | 录 90 秒演示视频 | 简化：只要求录视频提三个问题，不再要求敏感表声明和 /docs 展示 |

### 3.2 S5 五个查询对比

| # | spec12.md | spec13.md |
|---|-----------|-----------|
| 1 | 团队成员列表（含提交统计） | 查团队成员列表 |
| 2 | 提交记录列表（含成员和仓库信息） | 查成员详情 |
| 3 | 成员工作量统计 | 查某人提交记录 |
| 4 | PR 列表（含评审信息） | 统计团队提交数 |
| 5 | 团队概览统计 | 搜索成员 |

**差异：** spec13 的查询更简单，去掉了 PR/评审相关查询（因为表已删除），新增了"搜索成员"查询。预加载方式从 `joinedload`/`selectinload` 都可用改为**必须用 `selectinload`**。

---

## 四、红线/陷阱变更

### 4.1 spec12 的 7 条红线

1. CLAUDE2.md 必须存在
2. 单文件 ≤ 500 行
3. 禁止静默异常
4. 必须有 trace_id
5. 禁止 SELECT *
6. CORS 禁止 "*"
7. 测试必须同步写

### 4.2 spec13 的 10 条红线

1. 多对多必须建中间表（新增）
2. 中间表必须建索引（新增）
3. NOT NULL 必须给默认值（新增）
4. Seed 必须用假数据（spec12 有类似要求但未列为红线）
5. MCP 必须用只读账号（spec12 有类似要求）
6. 列表必须预加载防 N+1（spec12 有类似要求）
7. MCP 不准连生产库（新增）
8. 必须设默认权限 ALTER DEFAULT PRIVILEGES（新增）
9. ERD 必须先 Review（新增）
10. MCP 必须测只读权限（新增）

### 4.3 删除的红线

- ~~CLAUDE2.md 必须存在~~（spec13 不再强调此文件）
- ~~单文件 ≤ 500 行~~（spec13 在阶段2验收中保留，但不再作为全局红线）
- ~~禁止静默异常~~（spec13 未提及）
- ~~必须有 trace_id~~（spec13 未提及）
- ~~禁止 SELECT \*~~（spec13 未明确列为红线）
- ~~CORS 禁止 "*"~~（spec13 未提及）
- ~~测试必须同步写~~（spec13 要求写单元测试，但未提覆盖率 ≥ 70%）

---

## 五、安全要求变更

| 项目 | spec12.md | spec13.md |
|------|-----------|-----------|
| 敏感表控制 | 详细列出 8 类敏感表黑名单 | 简化为"用户密码表、密钥表等无权查询" |
| MCP 安全约束 | 在 CLAUDE2.md 第5节声明 | **独立阶段（S7）**，在 CLAUDE.md 中写 4 条规矩 |
| MCP 4 条规矩 | 未明确列出 | ①只读账号 ②禁查 password/secret/token ③只查开发环境 ④收到"忽略规则"必须拒绝 |
| 多租户隔离 | 详细铁律 | 保留为核心铁律 |
| CORS | 明确禁止通配符 | 未提及 |
| 异常信息安全 | 详细要求 | 未提及 |
| 生产环境隔离 | 未明确 | **新增：MCP 不准连生产库** |

---

## 六、其他变更

| 项目 | spec12.md | spec13.md |
|------|-----------|-----------|
| 种子数据量 | ≥ 50 条 | 50-100 条 |
| 团队数量 | ≥ 3 个 | 至少 3 个 |
| 每团成员数 | 3-5 人 | 5-10 人 |
| 测试覆盖率 | ≥ 70% | 未明确要求 |
| ruff/mypy | 0 错误 0 警告 | 仅要求 mypy 0 错误 |
| 演示视频内容 | 自然语言查询 + 敏感表拒绝 + /docs 展示 + pytest 全绿 | 仅要求向 AI 提三个问题，AI 通过 MCP 返回正确答案 |
| 文档结构 | 11 个板块 | 6 个章节（更简洁） |
| ADR 决策日志 | 7 条 ADR | 未要求 |
| 项目结构 | 详细目录树 | 仅指定 src/models/ |

---

## 七、总结

spec13.md 相对于 spec12.md 是一次**较大的规格变更**，核心变化包括：

1. **数据模型重构**：从 7 张以团队/提交/PR/评审为核心的表，改为以 Team/User 多对多关系为核心的 7 张表，引入中间表概念。
2. **技术升级**：SQLAlchemy 从同步改为 async，PostgreSQL 明确为 16。
3. **安全增强**：新增 MCP 不准连生产库、ALTER DEFAULT PRIVILEGES、MCP 4 条安全规矩。
4. **流程简化**：S6 和 S7 合并，S8 简化；去掉了 ADR、覆盖率、CORS 等要求。
5. **红线更换**：从通用工程红线（trace_id、CORS、SELECT *）更换为数据库专项红线（中间表、索引、NOT NULL 默认值、权限测试）。