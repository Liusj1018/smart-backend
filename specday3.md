项目安全规范：
1.项目名称：smart commit helper
项目目标：给该程序设置安全措施。解决现在谁都能调接口，删数据不安全，现在要做到，只有登录用户才能访问系统，管理员才能修改数据，普通成员只能看。任何人只能看到自己团队的数据。密码安全存储，泄露了也拿不到原文。
2.业务要求：
1.密码绝不能明文存。
2.token必须有有效期。
3.只有管理员才能改数据。
4.数据隔离是铁律。
5.登录失败统一回复。
6.错误信息不能暴露敏感信息
3.技术栈:
1.密码加密，bcrypt|cost=12，自动加盐。
2.token方案 JWT|Access Token 15min+Refresh Token 7D
3.签名算法：HS256|用环境变量里的“JWT_SECRET”签名
4.密钥管理：环境变量|所有secret走“.env”绝对不硬编码
5.权限检验：依赖注入|FastAPI的“Depends()”做统一拦截
6.行级权限：SQLAlchemy查询过滤|所有查询带“team_ID"+"OWNER_ID"过滤
7.CORS：白名单|只允许前端域名，不许设成“*”。
4.具体步骤：
S1：写一个加密工具，验证用户输入的密码和数据库里的乱码是否匹配。验收：同一个密码每次生成的乱码都不一样，但验证都能过。
S2：写一个注册端口，用户传邮箱，密码，团队编号。校验邮箱格式，密码长度至少8位。验收：用Bruno发注册请求，数据库里能看到新用户，“password”字段是一串乱码。
S3：任务:写一个登录接口，验证通过后签发:1Access Token(15分钟过期，用于访问接口)Refresh Token(7天过期，用于续期)验收:用正确的账号密码登录，拿到两个Token。
S4：任务:用户用RefreshToken换一张新的Access Token.Refresh Token 过期了就报错青重新登录’验收:拿有效 Refresh Token换新AccessToken，新的能正常访问接口.
S5:任务:写一个认证依赖，每次请求进来验证签名和过期时间，解析出用户编号，从数据库查出用户信息验收:访问带^Depends(current_user)的接口，不带Token报401。
S6：任务:写-个角色校验依赖，配合认证中间件校验当前用户角色，只允许管理员操作。使用，验收.:用普通成员的Token 调删除接口报403，用管理员，就成功调用token。
S7：任务:检查所有带‘id) 参数的接口，验收:必须校验当前用户是否有权访问这个数据，如果不匹配，返回404或403。
S8：1.所有接口的错误响应统一格式。2.随便发一个错误请求，返回的JSON格式统一，带追踪号
5.代码规范：需求拆解AI生成>人工校验提交归档;每步闭环后进入下一阶段，拒绝跳跃式开发。Agent仅负责代码生成，人工把控架构逻辑;每阶段代码需人工Review，严禁直接合并未验证代码。
6.注意事项：
1、JWT Secret硬编码在代码里/提交GitHub后密钥泄漏，任何人都能伪造Token/用环境变量，.env 加‘gitignore
2、JWT没有过期时间/Token永久有效，泄漏了永远能用
Access 15min, Refresh 7d
bcrypt (cost=12)
3、密码用MD5或SHA/GPU暴力破解极快，不安全/用
4、登录错误分情况提示/攻击者可枚举出哪些邮箱已注册
统一返回“用户名或密码错误”
5、只校验角色不校验归属/普通成员能看到别的团队数据
(IDOR)/查数据必须带‘team_id过滤
6、Payload里塞了密码/JWT是base64编码，任何人都能解
码看到/Payload只放^user_id'、role'、exp
7、CORS设成‘*/任何网站都能跨域调用你的APl/只开放
前端域名白名单
检测:旧Refresh被再次使用>整条链作废
8、Token被盗了没反应/攻击者可以一直用/加refresh旋转
9、错误信息返回堆栈/攻击者能看到你的代码结构/统一用HTTPException抛，不暴露traceback
10、测试里没用过期Token场景/正式上线后Token过期导致接口全崩|测试用例覆盖:正常、无Token、过期Token、权限不足
11.角色定义:资深安全审查员，严守 OWASP Top10+CWE标准,输出强制格式:漏洞D|OWASP编号|严重度|行号|修复方案|验证用例|必检25项:认证授权(8)·输入校验(5)·密钥管理(4)·依赖审计(3)#严重度分级:Critical(当天修)/High一周内)/Medium/Low强制规则:无把握项必须标记 ，提交人工兜底审查
标准化审计体系基于行业通用的OWASPTop 10与CWE缺陷标准，覆盖认证鉴权、输入验证、密钥管理等25项核心检查点，拒绝主观疏漏，确保审计维度无死角。
无缝融入研发流程输出结构化表格可直接粘贴至PR/MR评论区，团队一目了然。对Al无把握的风险点强制标记4 ，引入人工兜底审查，兼顾效率与严谨，提升代码上线安全性。一定要记住注意事项。