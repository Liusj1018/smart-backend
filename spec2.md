# Smart Commit Backend 项目说明书 (spec2.md)

---

## 一、项目目的

做一个叫 **"Smart Commit Backend"** 的工具，帮开发团队的管理员管理团队成员和代码提交记录。

### 解决什么问题
团队人多了以后，管理员搞不清"团队里到底有谁、每个人做了什么"，需要一个简单的后台工具来统一查看和管理。

### 核心规矩（多租户隔离）
- A团队的人登录后，**只能看到A团队的数据**，绝对看不到B团队的。
- 每个团队有自己的独立空间，互不干扰。

---

## 二、项目功能

### 功能1：查看成员列表
- **谁来用**：团队里的任何成员
- **做什么**：查看当前团队的所有成员，支持翻页和按条件筛选（比如只看后端组的）
- **输入什么**：想看第几页、每页显示几条、按什么条件筛选（可选）
- **输出什么**：当前页的成员名单（含每个成员的编号、名字、邮箱、角色、加入时间）
- **什么时候报错**：页码或每页条数填了负数 → 报错

### 功能2：查看某个成员的详细信息
- **谁来用**：团队里的任何成员
- **做什么**：查看某一个成员的完整资料
- **输入什么**：该成员的编号
- **输出什么**：该成员的全部信息（名字、邮箱、角色、加入时间、所属团队等）
- **什么时候报错**：输入的编号不存在 → 报错"查无此人"

### 完整八个接口（标准 CRUD）

| # | 接口 | 方法 | 路径 | 权限 |
|---|------|------|------|------|
| 1 | 创建成员 | POST | `/teams/{team_id}/members` | 管理员 |
| 2 | 查看成员列表（分页+筛选） | GET | `/teams/{team_id}/members` | 任何成员 |
| 3 | 查看成员详情 | GET | `/teams/{team_id}/members/{member_id}` | 任何成员 |
| 4 | 更新成员信息 | PUT | `/teams/{team_id}/members/{member_id}` | 管理员 |
| 5 | 删除成员 | DELETE | `/teams/{team_id}/members/{member_id}` | 管理员 |
| 6 | 查看提交记录列表 | GET | `/teams/{team_id}/commits` | 任何成员 |
| 7 | 查看提交记录详情 | GET | `/teams/{team_id}/commits/{commit_id}` | 任何成员 |
| 8 | 查看成员工作量统计 | GET | `/teams/{team_id}/members/{member_id}/workload` | 任何成员 |

---

## 三、核心约束（业务层面的死规矩）

1. *任何功能查询数据时，必须只查当前团队的数据。绝对不允许查出别的团队的数据。
2. **只有管理员能改数据**：添加、更新、删除、执行动作，这4类操作必须校验当前用户是否为管理员，否则一律拒绝。
3. **只读操作全员开放**：查看列表、查看详情，团队里任何成员都能用，不需要是管理员。
*多租户隔离是铁律**：
---

## 四、技术限制（技术选型上的硬性要求）

这些是写代码时必须遵守的技术规矩，和业务无关：

| # | 约束 | 说明 |
|---|------|------|
| 1 | 框架必须用 **FastAPI** | Python 版本 3.12 以上 |
| 2 | 数据格式用 **Pydantic** 定义 | 每个字段必须写明类型 |
| 3 | 类型检查必须 **0 错误** | 写完后跑 mypy，不能有任何红色报错 |
| 4 | 测试覆盖率必须 **≥ 70%** | 用 pytest 写测试，覆盖率不到 70% 不算完 |
| 5 | 单个代码文件 **≤ 500 行** | 超过了必须拆成多个文件 |
| 6 | 查数据时必须写明确字段 | 绝对不能用"星号"把全部字段拽出来 |
| 7 | 每个请求必须有 **追踪号 (trace_id)** | 自动生成，记日志，出错时返回给前端 |
| 8 | 报错格式必须统一 | 所有报错返回的 JSON 结构要长一样（RFC 7807） |
| 9 | 跨域 (CORS) 只开白名单 | 只允许前端地址访问，不许设成"星号" |
| 10 | 项目根目录必须有 **CLAUDE.md** | 写清楚技术栈和代码规矩，方便接手的人 |

---

## 五、完成步骤

| 步骤 | 内容 | 验收 |
|------|------|------|
| **第1步** | 在 `/routes` 目录下搭建本项目，建好 FastAPI 项目，分好文件夹，生成 CLAUDE.md | `/docs` 能打开 |
| **第2步** | Pydantic 写完 8 个接口的输入输出格式，跑通类型检查 | mypy 0 错误 |
| **第3步** | 8 个接口的业务逻辑写出来，数据先存在内存里，用调试工具挨个调通 | Bruno 全链路调通 |
| **第4步** | 配好全局报错处理，格式统一，每个请求带追踪号 | 错误返回统一格式 + trace_id |
| **第5步** | 用 pytest 写测试，跑通，覆盖率 ≥ 70% | pytest 全绿，覆盖率 ≥ 70% |
| **第6步** | 写 README，录 90 秒演示视频 | 新人 5 分钟跑起 |

---

## 六、验收标准

1. **类型检查**：跑完 mypy，输出显示"0 个错误"，截图为证。
2. **接口调通**：用 Bruno（接口调试工具）挨个调用 8 个接口，每个都能返回预期的数据。比如"查看成员列表"能返回一串成员名单。
3. **报错验证**：故意发一个错误请求（比如页码填 -1），返回的报错信息格式是统一的，而且带了一个"追踪号"。去日志里搜这个号，能搜到对应的报错记录。
4. **测试通过**：终端输出全部显示"通过"（绿色），并且覆盖率报告显示 ≥ 70%。

---

## 七、常见陷阱

| # | 陷阱 | 后果 | 规避措施 |
|---|------|------|----------|
| 1 | 漏写 CLAUDE.md | 后面接手的同事不知道项目规矩 | 第1步就生成，根目录必须有 |
| 2 | 单个文件超过 500 行 | 代码太臃肿，后期改不动 | 主动拆文件，每 500 行就切分 |
| 3 | 报错被吞掉不抛出来 | 线上出问题找不到原因 | 任何报错必须往上抛，不静默 |
| 4 | 缺追踪号 (trace_id) | 出问题无法从日志定位 | 请求自动生成 trace_id，写日志 |
| 5 | 查数据用"星号"查全部 | 又慢又浪费资源 | 只查需要的字段，写明列名 |
| 6 | 跨域设成"星号" | 安全隐患，任何网站都能调后端 | 只开前端地址的白名单 |
| 7 | 没有测试就交工 | 改一行代码都可能崩掉线上功能 | 功能写完同步写测试 |