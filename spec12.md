项目说明：
1.项目名称和背景
项目名称：Smart Commit Helper SaaS
背景：这个项目是 Smart Commit Helper后端的数据底座部分。
具体目标：
1.设计数据库结构（ERD）：画一张”藏宝图“，标清楚有哪些表，表里有什莫字段，表和表之间怎么关联。
2.把设计落地成代码：用SQLAlchemy把宝藏图翻译成代码。
3.生成Migration脚本：把代码里的结构同步到真实数据库里去，让数据库长出对应的表。
4.接入MCP数据工作流：让AI能通过对话直接查数据库，不用写SQL。
2.技术栈要求：
使用python的专属翻译器：SQLAlchemy。
3.任务交付：
S1：梳理7张核心业务表，绘制ERD并标注关联。验收：同伴通过Review，产出标准ERD文。
S2：编写SQLAlchemy，定义模型与索引约束。验收：所有模型文件生成完毕，模型映射准确。
S3：用Alembic生成Migration，编写up/down回滚逻辑。验收：Migration脚本生成成功，库表生效。
S4：编写seed.py，批量生成50+条业务数据。验收：可视化验证数据完整入库。
S5：封装5割个核心查询，消除N+1查询问题。验收：查询高效无冗余，单元测试全部通过。
S6：配置.mcp.json,创建数据库只读用户。验收：MCN Agent连通正常，查询无权限报错。
S7：/docs的8个接口直接从数据库读写，而不是读取内存。验收：在/docs可以直接读取数据库中的假业务数据。
S8：在Claude.md中声明禁止访问的敏感表；录制90sLoom视频，演示自然语言查询。验收：Agent无法越权查询，访问边界可控；视频清晰流畅，可分析。
4.避坑指南；
1 | 漏写 CLAUDE.md | 后面接手的同事不知道项目规矩 | 第1步就生成，根目录必须有 |
| 2 | 单个文件超过 500 行 | 代码太臃肿，后期改不动 | 主动拆文件，每 500 行就切分 |
| 3 | 报错被吞掉不抛出来 | 线上出问题找不到原因 | 任何报错必须往上抛，不静默 |
| 4 | 缺追踪号 (trace_id) | 出问题无法从日志定位 | 请求自动生成 trace_id，写日志 |
| 5 | 查数据用"星号"查全部 | 又慢又浪费资源 | 只查需要的字段，写明列名 |
| 6 | 跨域设成"星号" | 安全隐患，任何网站都能调后端 | 只开前端地址的白名单 |
| 7 | 没有测试就交工 | 改一行代码都可能崩掉线上功能 | 功能写完同步写测试 |