# Spec.md → Spec2.md 变更说明

本文档记录从原始 `Spec.md` 到详细版 `spec2.md` 的所有修改、补充和细化内容。

---

## 一、新增内容（Spec.md 中没有的）

### 1. 项目目的与问题定义
- **新增**：明确了项目要解决的具体问题——"团队人多了以后，管理员搞不清团队里有谁、每个人做了什么"。
- **新增**：以"谁来用、做什么、输入什么、输出什么、什么时候报错"的格式，对功能1和功能2做了详细的用例描述。

### 2. 多租户隔离的明确阐述
- **新增**：将多租户隔离从一句话背景提升为"核心规矩"，明确说明"A团队的人只能看到A团队的数据，绝对看不到B团队的"。
- **新增**：强调"每个团队有自己的独立空间，互不干扰"。

### 3. 权限控制模型
- **新增**：完整的权限规则——"只有管理员能改数据（增/删/改/执行动作），只读操作全员开放"。
- **新增**：8个接口表格中新增了"权限"列，标注每个接口是"管理员"还是"任何成员"可用。

### 4. 接口列表的明确化
- **新增**：列出了完整的8个接口表格，包含方法、路径、权限。Spec.md 只提到"标准CRUD 8接口"但未列出具体是哪8个。
- **新增**：明确了分页和筛选参数（页码、每页条数、筛选条件）。
- **新增**：明确了报错场景（页码/每页条数为负数、成员编号不存在→"查无此人"）。

### 5. 业务层面的核心约束（第三章）
- **新增**：独立章节"核心约束"，将业务规则从技术规则中分离出来，形成3条铁律：
  1. 多租户隔离是铁律
  2. 只有管理员能改数据
  3. 只读操作全员开放

### 6. 技术限制的细化（第四章）
- **新增**：Python 版本明确为 **3.12 以上**（Spec.md 未指定版本）。
- **新增**：报错格式明确引用 **RFC 7807** 标准。
- **新增**：第10条——项目根目录必须有 CLAUDE.md（Spec.md 只在避坑指南中提到）。
- 技术限制从 Spec.md 的零散描述整理为 **10条硬性要求表格**。

### 7. 验收标准的细化（第六章）
- **新增**：4条可量化的验收标准：
  1. mypy 0错误（截图为证）
  2. Bruno 挨个调用8个接口返回预期数据
  3. 错误请求返回统一格式 + trace_id，日志可追溯
  4. pytest 全绿 + 覆盖率 ≥ 70%
- Spec.md 的验收标准嵌在步骤中，spec2.md 独立成章且更具体。

### 8. 常见陷阱的扩充（第七章）
- 从 Spec.md 的 **4条** 扩充到 **7条**：
  - **新增第2条**：单个文件超过500行（Spec.md 只在避坑第1条中顺带提到）
  - **新增第7条**：没有测试就交工
- 每条陷阱都增加了"后果"和"规避措施"两列，更具指导性。

---

## 二、修改内容（与 Spec.md 表述不同的）

### 1. 项目名称
- **Spec.md**：`smart commit help backend`（有拼写错误 help）
- **spec2.md**：`Smart Commit Backend`（修正拼写，统一大小写）

### 2. 功能描述
- **Spec.md**：3个功能（看提交记录、查看工作量、管理仓库）
- **spec2.md**：聚焦为2个详细功能描述（查看成员列表、查看成员详情）+ 8个接口列表，将仓库管理和提交记录融入接口设计中，以成员管理为核心。

### 3. 完成步骤
- **Spec.md**：6个步骤（S1~S6），S3 与 S1 内容重复（"设立项目规范，搭好FastAPI项目"出现两次）
- **spec2.md**：6个步骤，修正了重复问题，每步增加了"验收"列，更清晰：
  - 第1步明确包含"生成 CLAUDE.md"
  - 第6步明确录屏时长为"90秒"

### 4. 避坑指南 → 常见陷阱
- **Spec.md**：叫"避坑指南"，4条，简单描述
- **spec2.md**：叫"常见陷阱"，7条，每条分"陷阱/后果/规避措施"三列，更结构化

---

## 三、保持不变的内容

以下核心要求在两个版本中保持一致：

| 项目 | 说明 |
|------|------|
| Web 框架 | FastAPI |
| 数据校验 | Pydantic，全类型覆盖 |
| 类型检查 | mypy 0 错误 |
| 测试框架 | pytest，覆盖率 ≥ 70% |
| 代码检查 | ruff + mypy |
| API 调试 | Bruno 全链路调通 |
| 数据存储 | 内存层闭环 |
| 路由规范 | 必须使用 `async def`，统一 REST 风格 |
| 异常格式 | RFC 7807 + trace-id |
| CORS | 禁止通配符 `"*"`，使用白名单 |
| 单文件限制 | ≤ 500 行 |
| 禁止静默异常 | 异常必须抛出，禁止 bare except |
| 交付物 | README + 演示视频 |

---

## 四、总结

| 维度 | Spec.md | spec2.md |
|------|---------|----------|
| 行数 | 18行 | ~120行 |
| 结构 | 简单列表 | 7个章节，结构化表格 |
| 接口定义 | 只提"8个CRUD接口" | 列出全部8个接口的方法/路径/权限 |
| 权限模型 | 未提及 | 管理员/普通成员两级权限 |
| 多租户隔离 | 一句话背景 | 独立章节，3条铁律 |
| 验收标准 | 嵌在步骤中 | 独立章节，4条可量化标准 |
| 陷阱/避坑 | 4条 | 7条，含后果和规避措施 |
| Python版本 | 未指定 | 3.12+ |