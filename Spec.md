1.项目名称：smart commit help backend
项目背景：前端已经做好了smart commit helper，在需要做她的后端，负责存储数据，处理业务逻辑，给前端提供接口。一个公司有很多员工，每个团队用自己的工具互相看不到对方数据，但是每个成员可以。
项目功能：1.看自己团队的代码提交记录。2.查看每个成员的工作量；3.管理自己团队的代码仓库。
2.技术栈
使用python进行项目骨架搭建，使用标准CRUD 8接口搭建。基于内存层完成闭环，通过Bruno全链路调试验证。
3.代码规范：路由必须必须使用async def，统一一个rest搭建风格。
4.流程：
S1：设立项目规范，在/routes目录下搭建本项目，搭好FastAPI项目，通过ruff/mypy检查。验收：/docs能打开。
S2：写完八个接口的Pydantic类型，全类型覆盖。验收：mypy0错。
S3：设立项目规范，搭好FastAPI项目，通过ruff/mypy检查
S4：配好全局异常，RFC7807格式和trace-id。验收：校验失败返回422+标准JSON格式。
S5：测试8个接口的用例，用pytest框架。每个接口至少测两种情况：正常情况和常见报错情况。验收：运行测试命令，全部通过，覆盖率报告达到70%以上。
S6：写好README和录屏。验收：新人五分钟跑起。
5.避坑指南：
1.漏写CLAUDE.md设计文档单文件超500行，破坏代码可读性与后期维护性。
2.异常被静默挑获不抛出，缺少tacce-id链路标识，导致线上故障排查无迹可寻。
3.滥用SELECT查询冗余数据，CORS跨域配置为通配符"，引发性能损耗与安全风险。
4.无自动化测试覆盖，无法保障代码质腿，导致选代频繁引发线上故障与回归风险。